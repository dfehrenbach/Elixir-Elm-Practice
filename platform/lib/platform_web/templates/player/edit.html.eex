<h2>Edit Player</h2>

<%= form_for @changeset, player_path(@conn, :update, @player), fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <div class="form-group">
    <%= label f, :display_name, "Change Display Name", class: "control-label" %>
    <%= text_input f, :display_name, "Change Display Name", placeholder: "Enter display name...", class: "control-label" %>
    <%= error f, :display_name %>
  </div>
  <div class="form-group">
    <%= label f, :username, "Change Username", class: "control-label" %>
    <%= text_input f, :username, "Change Username", placeholder: "Enter username...", class: "control-label" %>
    <%= error f, :username %>
  </div>
  <div class="form-group">
    <%= label f, :password, "Change Password", class: "control-label" %>
    <%= text_input f, :password, "Change Password", placeholder: "Enter password...", class: "control-label" %>
    <%= error f, :password %>
  </div>
  <div class="form-group">
    <%= submit "Submit", class: "btn btn-primary" %>
    <span><%= link "Back", to: page_path(@conn, :index), class: "btn btn-default" %></span>
  </div>
<% end %>
<%= render "form.html", Map.put(assigns, :action, player_path(@conn, :update, @player)) %>

<span><%= link "Back", to: player_path(@conn, :index) %></span>
